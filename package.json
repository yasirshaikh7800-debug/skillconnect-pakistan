{
  "name": "skillconnect-pakistan",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/frontend",
    "build": "rm -rf .next_build .next dist apps/frontend/dist apps/frontend/.next_build && NEXT_DIST_DIR=.next_build NODE_ENV=production npm run build --workspace=apps/frontend && mkdir -p .next dist apps/frontend/dist && cp -a apps/frontend/.next_build/. .next/ && cp -a apps/frontend/.next_build/. dist/ && cp -a apps/frontend/.next_build/. apps/frontend/dist/ && rm -rf apps/frontend/.next_build",
    "start": "node server.ts",
    "lint": "npm run lint --workspace=apps/frontend"
  },
  "devDependencies": {
    "eslint": "^10.8.1",
    "eslint-config-next": "^16.3.0"
  },
  "dependencies": {
    "@google/genai": "^2.16.0",
    "@types/three": "^0.185.4",
    "next": "15.0.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "three": "^0.185.1"
  }
}
